public class QuoteService implements IQuoteService {
    public Integer  getTotalAcceptedQuotes(List<sObject> records){
        system.debug('getTotalAcceptedQuotes here we are incrementing shit!!');
        return 1;
    }
}